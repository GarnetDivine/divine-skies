warning: in the working copy of 'src/main/java/com/skycycle/HdBridge.java', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/main/java/com/skycycle/SceneTintOverlay.java', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/main/java/com/skycycle/SkyCycleConfig.java', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/main/java/com/skycycle/SkyCycleOverlay.java', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/main/java/com/skycycle/SkyCyclePlugin.java', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/main/java/com/skycycle/UndergroundDetector.java', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/main/java/com/skycycle/HdBridge.java b/src/main/java/com/skycycle/HdBridge.java[m
[1mindex c934eff..85ce97b 100644[m
[1m--- a/src/main/java/com/skycycle/HdBridge.java[m
[1m+++ b/src/main/java/com/skycycle/HdBridge.java[m
[36m@@ -42,11 +42,13 @@[m [mpublic class HdBridge[m
     private static final String KEY_OVERRIDE_SKY = "overrideSky";[m
     private static final String KEY_BRIGHTNESS = "screenBrightness";[m
     private static final String KEY_CONTRAST = "fContrast";[m
[32m+[m[32m    private static final String KEY_SHADOW_MODE = "shadowMode"; // OFF / FAST / DETAILED[m
 [m
     // Keys in our own config group for persisted baselines[m
     private static final String DS_GROUP = SkyCycleConfig.CONFIG_GROUP;[m
     private static final String DS_BASE_BRIGHTNESS = "hdBaseBrightness";[m
     private static final String DS_BASE_CONTRAST = "hdBaseContrast";[m
[32m+[m[32m    private static final String DS_BASE_SHADOW_MODE = "hdBaseShadowMode";[m
     private static final String DS_ORIG_DEFAULT_SKY = "hdOrigDefaultSky";[m
     private static final String DS_ORIG_OVERRIDE_SKY = "hdOrigOverrideSky";[m
     private static final String DS_DIRTY = "hdDirty";[m
[36m@@ -64,9 +66,13 @@[m [mpublic class HdBridge[m
     private int baseBrightness = 100;[m
     private int baseContrast = 100;[m
 [m
[32m+[m[32m    // The user's true base shadow mode from 117 HD (OFF / FAST / DETAILED)[m
[32m+[m[32m    private String baseShadowMode = "DETAILED";[m
[32m+[m
     // Track the last offsets we applied so we can detect external 117HD changes[m
     private int lastAppliedBrightness = -1;[m
     private int lastAppliedContrast = -1;[m
[32m+[m[32m    private String lastAppliedShadowMode = null;[m
 [m
     @Inject[m
     public HdBridge(ConfigManager configManager, Client client)[m
[36m@@ -108,6 +114,7 @@[m [mpublic class HdBridge[m
             // Previous session didn't shut down cleanly — use our persisted baselines[m
             baseBrightness = readOurIntConfig(DS_BASE_BRIGHTNESS, 100);[m
             baseContrast = readOurIntConfig(DS_BASE_CONTRAST, 100);[m
[32m+[m[32m            baseShadowMode = readOurStringConfig(DS_BASE_SHADOW_MODE, "DETAILED");[m
             origDefaultSky = configManager.getConfiguration(DS_GROUP, DS_ORIG_DEFAULT_SKY);[m
             origOverrideSky = configManager.getConfiguration(DS_GROUP, DS_ORIG_OVERRIDE_SKY);[m
 [m
[36m@@ -118,6 +125,7 @@[m [mpublic class HdBridge[m
             // Immediately write the correct baselines back to 117HD so it's in a known state[m
             configManager.setConfiguration(HD_GROUP, KEY_BRIGHTNESS, String.valueOf(baseBrightness));[m
             configManager.setConfiguration(HD_GROUP, KEY_CONTRAST, String.valueOf(baseContrast));[m
[32m+[m[32m            configManager.setConfiguration(HD_GROUP, KEY_SHADOW_MODE, baseShadowMode);[m
             if (origDefaultSky != null)[m
             {[m
                 configManager.setConfiguration(HD_GROUP, KEY_DEFAULT_SKY, origDefaultSky);[m
[36m@@ -134,10 +142,12 @@[m [mpublic class HdBridge[m
             origOverrideSky = configManager.getConfiguration(HD_GROUP, KEY_OVERRIDE_SKY);[m
             baseBrightness = readIntConfig(KEY_BRIGHTNESS, 100);[m
             baseContrast = readIntConfig(KEY_CONTRAST, 100);[m
[32m+[m[32m            baseShadowMode = readStringConfig(KEY_SHADOW_MODE, "DETAILED");[m
 [m
             // Persist these as our known-good baselines[m
             configManager.setConfiguration(DS_GROUP, DS_BASE_BRIGHTNESS, String.valueOf(baseBrightness));[m
             configManager.setConfiguration(DS_GROUP, DS_BASE_CONTRAST, String.valueOf(baseContrast));[m
[32m+[m[32m            configManager.setConfiguration(DS_GROUP, DS_BASE_SHADOW_MODE, baseShadowMode);[m
             if (origDefaultSky != null)[m
             {[m
                 configManager.setConfiguration(DS_GROUP, DS_ORIG_DEFAULT_SKY, origDefaultSky);[m
[36m@@ -159,6 +169,7 @@[m [mpublic class HdBridge[m
         settingsSaved = true;[m
         lastAppliedBrightness = -1;[m
         lastAppliedContrast = -1;[m
[32m+[m[32m        lastAppliedShadowMode = null;[m
     }[m
 [m
     /**[m
[36m@@ -178,9 +189,10 @@[m [mpublic class HdBridge[m
         }[m
         client.setSkyboxColor(origSkyboxColor);[m
 [m
[31m-        // Restore original brightness/contrast[m
[32m+[m[32m        // Restore original brightness/contrast/shadows[m
         configManager.setConfiguration(HD_GROUP, KEY_BRIGHTNESS, String.valueOf(baseBrightness));[m
         configManager.setConfiguration(HD_GROUP, KEY_CONTRAST, String.valueOf(baseContrast));[m
[32m+[m[32m        configManager.setConfiguration(HD_GROUP, KEY_SHADOW_MODE, baseShadowMode);[m
 [m
         // Clean shutdown — clear the dirty flag so next startup reads fresh from 117HD[m
         configManager.setConfiguration(DS_GROUP, DS_DIRTY, "false");[m
[36m@@ -188,6 +200,7 @@[m [mpublic class HdBridge[m
         settingsSaved = false;[m
         lastAppliedBrightness = -1;[m
         lastAppliedContrast = -1;[m
[32m+[m[32m        lastAppliedShadowMode = null;[m
         log.debug("Restored 117HD settings and cleared dirty flag");[m
     }[m
 [m
[36m@@ -207,10 +220,19 @@[m [mpublic class HdBridge[m
         baseBrightness = readIntConfig(KEY_BRIGHTNESS, 100);[m
         baseContrast = readIntConfig(KEY_CONTRAST, 100);[m
 [m
[32m+[m[32m        // Only recapture the shadow mode if we are NOT currently forcing a night shadow[m
[32m+[m[32m        // value — otherwise we'd capture our own override as the user's baseline.[m
[32m+[m[32m        if (lastAppliedShadowMode == null || lastAppliedShadowMode.equals(baseShadowMode))[m
[32m+[m[32m        {[m
[32m+[m[32m            baseShadowMode = readStringConfig(KEY_SHADOW_MODE, "DETAILED");[m
[32m+[m[32m            configManager.setConfiguration(DS_GROUP, DS_BASE_SHADOW_MODE, baseShadowMode);[m
[32m+[m[32m        }[m
[32m+[m
         configManager.setConfiguration(DS_GROUP, DS_BASE_BRIGHTNESS, String.valueOf(baseBrightness));[m
         configManager.setConfiguration(DS_GROUP, DS_BASE_CONTRAST, String.valueOf(baseContrast));[m
 [m
[31m-        log.info("Recaptured 117HD baselines: brightness={}, contrast={}", baseBrightness, baseContrast);[m
[32m+[m[32m        log.info("Recaptured 117HD baselines: brightness={}, contrast={}, shadows={}",[m
[32m+[m[32m            baseBrightness, baseContrast, baseShadowMode);[m
     }[m
 [m
     /**[m
[36m@@ -252,6 +274,32 @@[m [mpublic class HdBridge[m
         lastAppliedContrast = newContrast;[m
     }[m
 [m
[32m+[m[32m    /**[m
[32m+[m[32m     * Set 117 HD's shadow mode, or restore the user's baseline.[m
[32m+[m[32m     *[m
[32m+[m[32m     * <p>Skips redundant writes internally, so it is safe to call every update tick.</p>[m
[32m+[m[32m     *[m
[32m+[m[32m     * @param mode "OFF" or "FAST" to force a night shadow mode, or null to restore[m
[32m+[m[32m     *             the user's captured baseline (day / underground / POH / shutdown).[m
[32m+[m[32m     */[m
[32m+[m[32m    public void applyShadowMode(String mode)[m
[32m+[m[32m    {[m
[32m+[m[32m        if (!settingsSaved)[m
[32m+[m[32m        {[m
[32m+[m[32m            return;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        String target = (mode == null) ? baseShadowMode : mode;[m
[32m+[m[32m        if (target.equals(lastAppliedShadowMode))[m
[32m+[m[32m        {[m
[32m+[m[32m            return;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        log.debug("SkyCycle shadow mode: base={}, applying={}", baseShadowMode, target);[m
[32m+[m[32m        configManager.setConfiguration(HD_GROUP, KEY_SHADOW_MODE, target);[m
[32m+[m[32m        lastAppliedShadowMode = target;[m
[32m+[m[32m    }[m
[32m+[m
     /**[m
      * Get the persisted base brightness (for use by the overlay or other display).[m
      */[m
[36m@@ -268,6 +316,18 @@[m [mpublic class HdBridge[m
         return baseContrast;[m
     }[m
 [m
[32m+[m[32m    private String readStringConfig(String key, String defaultValue)[m
[32m+[m[32m    {[m
[32m+[m[32m        String val = configManager.getConfiguration(HD_GROUP, key);[m
[32m+[m[32m        return (val == null || val.isEmpty()) ? defaultValue : val;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    private String readOurStringConfig(String key, String defaultValue)[m
[32m+[m[32m    {[m
[32m+[m[32m        String val = configManager.getConfiguration(DS_GROUP, key);[m
[32m+[m[32m        return (val == null || val.isEmpty()) ? defaultValue : val;[m
[32m+[m[32m    }[m
[32m+[m
     private int readIntConfig(String key, int defaultValue)[m
     {[m
         String val = configManager.getConfiguration(HD_GROUP, key);[m
[1mdiff --git a/src/main/java/com/skycycle/SceneTintOverlay.java b/src/main/java/com/skycycle/SceneTintOverlay.java[m
[1mindex 6255898..d641909 100644[m
[1m--- a/src/main/java/com/skycycle/SceneTintOverlay.java[m
[1m+++ b/src/main/java/com/skycycle/SceneTintOverlay.java[m
[36m@@ -58,14 +58,9 @@[m [mpublic class SceneTintOverlay extends Overlay[m
             return null;[m
         }[m
 [m
[31m-        // Don't tint when underground (underground has its own lighting)[m
[31m-        if (plugin.isCurrentlyUnderground())[m
[31m-        {[m
[31m-            return null;[m
[31m-        }[m
[31m-[m
[31m-        Color tintColor = getTintColor();[m
[31m-        if (tintColor == null)[m
[32m+[m[32m        // Don't tint underground or in the POH — those areas use their own[m
[32m+[m[32m        // lighting / custom skybox, and a day/night tint there looks wrong.[m
[32m+[m[32m        if (plugin.isCurrentlyUnderground() || plugin.isCurrentlyInPoh())[m
         {[m
             return null;[m
         }[m
[36m@@ -76,19 +71,17 @@[m [mpublic class SceneTintOverlay extends Overlay[m
             return null;[m
         }[m
 [m
[31m-        // Alpha ranges from 0 (invisible) to 255 (solid)[m
[31m-        // We cap at ~60% opacity even at strength 100 because full solid looks terrible[m
[31m-        int alpha = (int) Math.round(strength / 100.0 * 153);[m
[31m-        alpha = Math.max(0, Math.min(153, alpha));[m
[32m+[m[32m        // Master alpha: 0 (invisible) to ~60% opacity at strength 100,[m
[32m+[m[32m        // because a fully solid fill looks terrible.[m
[32m+[m[32m        int masterAlpha = clampAlpha((int) Math.round(strength / 100.0 * 153));[m
 [m
[31m-        Color overlayColor = new Color([m
[31m-            tintColor.getRed(),[m
[31m-            tintColor.getGreen(),[m
[31m-            tintColor.getBlue(),[m
[31m-            alpha[m
[31m-        );[m
[32m+[m[32m        Color overlayColor = getTintColor(masterAlpha);[m
[32m+[m[32m        if (overlayColor == null || overlayColor.getAlpha() <= 0)[m
[32m+[m[32m        {[m
[32m+[m[32m            return null;[m
[32m+[m[32m        }[m
 [m
[31m-        // Fill the entire game canvas[m
[32m+[m[32m        // Fill the entire game canvas (UI widgets render above this layer)[m
         graphics.setColor(overlayColor);[m
         graphics.fillRect(0, 0, client.getCanvasWidth(), client.getCanvasHeight());[m
 [m
[36m@@ -96,33 +89,51 @@[m [mpublic class SceneTintOverlay extends Overlay[m
     }[m
 [m
     /**[m
[31m-     * Determine the current tint color based on the configured mode.[m
[32m+[m[32m     * Determine the current tint color AND alpha based on the configured mode.[m
[32m+[m[32m     *[m
[32m+[m[32m     * <p>In CYCLE mode each phase has its own effective alpha: a disabled phase tint[m
[32m+[m[32m     * means alpha 0 for that phase (previously a disabled day tint would incorrectly[m
[32m+[m[32m     * borrow the night tint at full strength, tinting the daytime scene). During[m
[32m+[m[32m     * sunrise/sunset both the color and the alpha are smoothly interpolated, so a[m
[32m+[m[32m     * night-only tint now fades in over the sunset instead of popping.</p>[m
[32m+[m[32m     *[m
[32m+[m[32m     * @param masterAlpha the alpha corresponding to the configured tint strength[m
[32m+[m[32m     * @return the premixed overlay color with alpha, or null for no overlay[m
      */[m
[31m-    private Color getTintColor()[m
[32m+[m[32m    private Color getTintColor(int masterAlpha)[m
     {[m
         SceneTintMode mode = config.sceneTintMode();[m
 [m
         if (mode == SceneTintMode.FIXED)[m
         {[m
[31m-            return config.sceneTintFixedColor();[m
[32m+[m[32m            Color fixed = config.sceneTintFixedColor();[m
[32m+[m[32m            return withAlpha(fixed, masterAlpha);[m
         }[m
 [m
         // CYCLE mode: follow the day/night tint colors[m
         CycleEngine engine = plugin.getCycleEngine();[m
[31m-        if (engine == null) return null;[m
[32m+[m[32m        if (engine == null)[m
[32m+[m[32m        {[m
[32m+[m[32m            return null;[m
[32m+[m[32m        }[m
 [m
         Color dayTint = config.dayTintEnabled() ? config.dayTintColor() : null;[m
         Color nightTint = config.nightTintEnabled() ? config.nightTintColor() : null;[m
 [m
[31m-        // If neither day nor night tint is enabled, fall back to the transition color[m
[32m+[m[32m        // Neither tint enabled: nothing to draw. (Previously this fell back to the[m
[32m+[m[32m        // transition color, permanently orange-tinting the scene — surprising.)[m
         if (dayTint == null && nightTint == null)[m
         {[m
[31m-            return config.transitionColor();[m
[32m+[m[32m            return null;[m
         }[m
 [m
[31m-        // If only one is set, use it for both ends[m
[31m-        if (dayTint == null) dayTint = nightTint;[m
[31m-        if (nightTint == null) nightTint = dayTint;[m
[32m+[m[32m        int dayAlpha = dayTint != null ? masterAlpha : 0;[m
[32m+[m[32m        int nightAlpha = nightTint != null ? masterAlpha : 0;[m
[32m+[m
[32m+[m[32m        // For color interpolation continuity, a disabled end borrows the other end's[m
[32m+[m[32m        // hue — but its alpha stays 0, so it never actually shows during that phase.[m
[32m+[m[32m        Color dayColor = dayTint != null ? dayTint : nightTint;[m
[32m+[m[32m        Color nightColor = nightTint != null ? nightTint : dayTint;[m
 [m
         CyclePhase phase = engine.getCurrentPhase();[m
         double progress = engine.getPhaseProgress();[m
[36m@@ -130,21 +141,41 @@[m [mpublic class SceneTintOverlay extends Overlay[m
         switch (phase)[m
         {[m
             case DAY:[m
[31m-                return dayTint;[m
[32m+[m[32m                return dayAlpha > 0 ? withAlpha(dayColor, dayAlpha) : null;[m
             case NIGHT:[m
[31m-                return nightTint;[m
[32m+[m[32m                return nightAlpha > 0 ? withAlpha(nightColor, nightAlpha) : null;[m
             case SUNSET:[m
             {[m
[31m-                double t = progress * progress * (3 - 2 * progress); // smoothstep[m
[31m-                return CycleEngine.lerpColor(dayTint, nightTint, t);[m
[32m+[m[32m                double t = smoothstep(progress);[m
[32m+[m[32m                Color c = CycleEngine.lerpColor(dayColor, nightColor, t);[m
[32m+[m[32m                int a = (int) Math.round(dayAlpha + (nightAlpha - dayAlpha) * t);[m
[32m+[m[32m                return withAlpha(c, clampAlpha(a));[m
             }[m
             case SUNRISE:[m
             {[m
[31m-                double t = progress * progress * (3 - 2 * progress);[m
[31m-                return CycleEngine.lerpColor(nightTint, dayTint, t);[m
[32m+[m[32m                double t = smoothstep(progress);[m
[32m+[m[32m                Color c = CycleEngine.lerpColor(nightColor, dayColor, t);[m
[32m+[m[32m                int a = (int) Math.round(nightAlpha + (dayAlpha - nightAlpha) * t);[m
[32m+[m[32m                return withAlpha(c, clampAlpha(a));[m
             }[m
             default:[m
[31m-                return dayTint;[m
[32m+[m[32m                return dayAlpha > 0 ? withAlpha(dayColor, dayAlpha) : null;[m
         }[m
     }[m
[32m+[m
[32m+[m[32m    private static double smoothstep(double t)[m
[32m+[m[32m    {[m
[32m+[m[32m        t = Math.max(0, Math.min(1, t));[m
[32m+[m[32m        return t * t * (3 - 2 * t);[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    private static int clampAlpha(int a)[m
[32m+[m[32m    {[m
[32m+[m[32m        return Math.max(0, Math.min(153, a));[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    private static Color withAlpha(Color c, int alpha)[m
[32m+[m[32m    {[m
[32m+[m[32m        return new Color(c.getRed(), c.getGreen(), c.getBlue(), alpha);[m
[32m+[m[32m    }[m
 }[m
[1mdiff --git a/src/main/java/com/skycycle/SkyCycleConfig.java b/src/main/java/com/skycycle/SkyCycleConfig.java[m
[1mindex 456e922..ee75504 100644[m
[1m--- a/src/main/java/com/skycycle/SkyCycleConfig.java[m
[1m+++ b/src/main/java/com/skycycle/SkyCycleConfig.java[m
[36m@@ -17,6 +17,7 @@[m [mpackage com.skycycle;[m
 import net.runelite.client.config.*;[m
 [m
 import java.awt.Color;[m
[32m+[m[32mimport java.awt.event.KeyEvent;[m
 [m
 @ConfigGroup(SkyCycleConfig.CONFIG_GROUP)[m
 public interface SkyCycleConfig extends Config[m
[36m@@ -337,6 +338,20 @@[m [mpublic interface SkyCycleConfig extends Config[m
         return 20;[m
     }[m
 [m
[32m+[m[32m    @ConfigItem([m
[32m+[m[32m        keyName = "nightShadowMode",[m
[32m+[m[32m        name = "Night Shadows",[m
[32m+[m[32m        description = "Control 117 HD's shadows during nighttime. 'No Shadows' removes them, " +[m
[32m+[m[32m            "'Reduced (Fast)' switches to 117 HD's softer fast shadows, 'Unchanged' leaves your " +[m
[32m+[m[32m            "117 HD setting alone. Your normal shadows return at sunrise.",[m
[32m+[m[32m        section = nightSection,[m
[32m+[m[32m        position = 6[m
[32m+[m[32m    )[m
[32m+[m[32m    default NightShadowMode nightShadowMode()[m
[32m+[m[32m    {[m
[32m+[m[32m        return NightShadowMode.OFF;[m
[32m+[m[32m    }[m
[32m+[m
     // =============================================[m
     // Transition[m
     // =============================================[m
[36m@@ -579,6 +594,67 @@[m [mpublic interface SkyCycleConfig extends Config[m
         return false;[m
     }[m
 [m
[32m+[m[32m    // =============================================[m
[32m+[m[32m    // Manual Overrides[m
[32m+[m[32m    // =============================================[m
[32m+[m
[32m+[m[32m    @ConfigSection([m
[32m+[m[32m        name = "Manual Overrides",[m
[32m+[m[32m        description = "Manually correct how an area is classified. Useful when automatic " +[m
[32m+[m[32m            "detection gets an area wrong. Overrides are saved per region and persist between sessions.",[m
[32m+[m[32m        position = 8[m
[32m+[m[32m    )[m
[32m+[m[32m    String overrideSection = "overrideSection";[m
[32m+[m
[32m+[m[32m    @ConfigItem([m
[32m+[m[32m        keyName = "allowManualOverrides",[m
[32m+[m[32m        name = "Enable Manual Overrides",[m
[32m+[m[32m        description = "Allow the toggle hotkey to manually set the area type for the region you're standing in.",[m
[32m+[m[32m        section = overrideSection,[m
[32m+[m[32m        position = 0[m
[32m+[m[32m    )[m
[32m+[m[32m    default boolean allowManualOverrides()[m
[32m+[m[32m    {[m
[32m+[m[32m        return true;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    @ConfigItem([m
[32m+[m[32m        keyName = "toggleAreaTypeKey",[m
[32m+[m[32m        name = "Toggle Area Type",[m
[32m+[m[32m        description = "Cycles the CURRENT region: Auto -> Underground -> Surface -> Player House -> Auto. " +[m
[32m+[m[32m            "The choice is saved for that region.",[m
[32m+[m[32m        section = overrideSection,[m
[32m+[m[32m        position = 1[m
[32m+[m[32m    )[m
[32m+[m[32m    default Keybind toggleAreaTypeKey()[m
[32m+[m[32m    {[m
[32m+[m[32m        return new Keybind(KeyEvent.VK_K, KeyEvent.SHIFT_DOWN_MASK);[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    @ConfigItem([m
[32m+[m[32m        keyName = "overrideChatFeedback",[m
[32m+[m[32m        name = "Chat Feedback",[m
[32m+[m[32m        description = "Print a game chat message confirming the new area type when you press the toggle hotkey.",[m
[32m+[m[32m        section = overrideSection,[m
[32m+[m[32m        position = 2[m
[32m+[m[32m    )[m
[32m+[m[32m    default boolean overrideChatFeedback()[m
[32m+[m[32m    {[m
[32m+[m[32m        return true;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    @ConfigItem([m
[32m+[m[32m        keyName = "clearAllOverrides",[m
[32m+[m[32m        name = "Clear All Overrides",[m
[32m+[m[32m        description = "Remove every saved manual override and return all regions to automatic detection.",[m
[32m+[m[32m        section = overrideSection,[m
[32m+[m[32m        position = 3[m
[32m+[m[32m    )[m
[32m+[m[32m    default boolean clearAllOverrides()[m
[32m+[m[32m    {[m
[32m+[m[32m        return false;[m
[32m+[m[32m    }[m
[32m+[m
     // =============================================[m
     // Advanced[m
     // =============================================[m
[36m@@ -586,7 +662,7 @@[m [mpublic interface SkyCycleConfig extends Config[m
     @ConfigSection([m
         name = "Advanced",[m
         description = "Advanced configuration options.",[m
[31m-        position = 8,[m
[32m+[m[32m        position = 9,[m
         closedByDefault = true[m
     )[m
     String advancedSection = "advancedSection";[m
[1mdiff --git a/src/main/java/com/skycycle/SkyCycleOverlay.java b/src/main/java/com/skycycle/SkyCycleOverlay.java[m
[1mindex 13c1446..badd497 100644[m
[1m--- a/src/main/java/com/skycycle/SkyCycleOverlay.java[m
[1m+++ b/src/main/java/com/skycycle/SkyCycleOverlay.java[m
[36m@@ -128,24 +128,49 @@[m [mpublic class SkyCycleOverlay extends Overlay[m
             // Developer Mode: show region/instance/plane info for community bug reporting[m
             if (config.devMode())[m
             {[m
[32m+[m[32m                UndergroundDetector detector = plugin.getUndergroundDetector();[m
                 int[] regions = plugin.getClient().getMapRegions();[m
[32m+[m
[32m+[m[32m                int resolved = detector.getResolvedRegion();[m
[32m+[m[32m                panelComponent.getChildren().add(LineComponent.builder()[m
[32m+[m[32m                    .left("Region (resolved):")[m
[32m+[m[32m                    .right(resolved >= 0 ? String.valueOf(resolved) : "—")[m
[32m+[m[32m                    .rightColor(new Color(200, 200, 120))[m
[32m+[m[32m                    .build());[m
[32m+[m
                 if (regions != null)[m
                 {[m
                     panelComponent.getChildren().add(LineComponent.builder()[m
[31m-                        .left("Region:")[m
[32m+[m[32m                        .left("Loaded:")[m
                         .right(java.util.Arrays.toString(regions))[m
                         .rightColor(new Color(180, 180, 180))[m
                         .build());[m
[32m+[m[32m                }[m
 [m
[32m+[m[32m                panelComponent.getChildren().add(LineComponent.builder()[m
[32m+[m[32m                    .left("Instance:")[m
[32m+[m[32m                    .right(String.valueOf(plugin.getClient().isInInstancedRegion()))[m
[32m+[m[32m                    .rightColor(new Color(180, 180, 180))[m
[32m+[m[32m                    .build());[m
[32m+[m
[32m+[m[32m                panelComponent.getChildren().add(LineComponent.builder()[m
[32m+[m[32m                    .left("Plane:")[m
[32m+[m[32m                    .right(String.valueOf(plugin.getClient().getPlane()))[m
[32m+[m[32m                    .rightColor(new Color(180, 180, 180))[m
[32m+[m[32m                    .build());[m
[32m+[m
[32m+[m[32m                AreaType override = detector.getCurrentOverride();[m
[32m+[m[32m                panelComponent.getChildren().add(LineComponent.builder()[m
[32m+[m[32m                    .left("Override:")[m
[32m+[m[32m                    .right(override == null ? "none (auto)" : override.getDisplayName())[m
[32m+[m[32m                    .rightColor(override == null ? new Color(140, 140, 140) : new Color(120, 220, 140))[m
[32m+[m[32m                    .build());[m
[32m+[m
[32m+[m[32m                if (detector.getOverrideCount() > 0)[m
[32m+[m[32m                {[m
                     panelComponent.getChildren().add(LineComponent.builder()[m
[31m-                        .left("Instance:")[m
[31m-                        .right(String.valueOf(plugin.getClient().isInInstancedRegion()))[m
[31m-                        .rightColor(new Color(180, 180, 180))[m
[31m-                        .build());[m
[31m-[m
[31m-                    panelComponent.getChildren().add(LineComponent.builder()[m
[31m-                        .left("Plane:")[m
[31m-                        .right(String.valueOf(plugin.getClient().getPlane()))[m
[32m+[m[32m                        .left("Saved overrides:")[m
[32m+[m[32m                        .right(String.valueOf(detector.getOverrideCount()))[m
                         .rightColor(new Color(180, 180, 180))[m
                         .build());[m
                 }[m
[1mdiff --git a/src/main/java/com/skycycle/SkyCyclePlugin.java b/src/main/java/com/skycycle/SkyCyclePlugin.java[m
[1mindex a6245af..88f5cea 100644[m
[1m--- a/src/main/java/com/skycycle/SkyCyclePlugin.java[m
[1m+++ b/src/main/java/com/skycycle/SkyCyclePlugin.java[m
[36m@@ -17,16 +17,20 @@[m [mpackage com.skycycle;[m
 import com.google.inject.Provides;[m
 import org.slf4j.Logger;[m
 import org.slf4j.LoggerFactory;[m
[32m+[m[32mimport net.runelite.api.ChatMessageType;[m
 import net.runelite.api.Client;[m
 import net.runelite.api.GameState;[m
 import net.runelite.api.events.GameStateChanged;[m
 import net.runelite.api.events.GameTick;[m
[32m+[m[32mimport net.runelite.client.callback.ClientThread;[m
 import net.runelite.client.config.ConfigManager;[m
 import net.runelite.client.eventbus.Subscribe;[m
 import net.runelite.client.events.ConfigChanged;[m
[32m+[m[32mimport net.runelite.client.input.KeyManager;[m
 import net.runelite.client.plugins.Plugin;[m
 import net.runelite.client.plugins.PluginDescriptor;[m
 import net.runelite.client.ui.overlay.OverlayManager;[m
[32m+[m[32mimport net.runelite.client.util.HotkeyListener;[m
 [m
 import javax.inject.Inject;[m
 import java.awt.Color;[m
[36m@@ -49,6 +53,19 @@[m [mpublic class SkyCyclePlugin extends Plugin[m
     @Inject private UndergroundDetector undergroundDetector;[m
     @Inject private SkyCycleOverlay overlay;[m
     @Inject private SceneTintOverlay sceneTintOverlay;[m
[32m+[m[32m    @Inject private KeyManager keyManager;[m
[32m+[m[32m    @Inject private ClientThread clientThread;[m
[32m+[m[32m    @Inject private ConfigManager configManager;[m
[32m+[m
[32m+[m[32m    private final HotkeyListener toggleAreaHotkey = new HotkeyListener(() -> config.toggleAreaTypeKey())[m
[32m+[m[32m    {[m
[32m+[m[32m        @Override[m
[32m+[m[32m        public void hotkeyPressed()[m
[32m+[m[32m        {[m
[32m+[m[32m            // Hotkey fires on the AWT thread; region/coordinate reads must happen on the client thread.[m
[32m+[m[32m            clientThread.invoke(SkyCyclePlugin.this::handleToggleArea);[m
[32m+[m[32m        }[m
[32m+[m[32m    };[m
 [m
     private CycleEngine cycleEngine;[m
     private boolean currentlyUnderground = false;[m
[36m@@ -65,6 +82,7 @@[m [mpublic class SkyCyclePlugin extends Plugin[m
     public boolean isCurrentlyUnderground() { return currentlyUnderground; }[m
     public boolean isCurrentlyInPoh() { return currentlyInPoh; }[m
     public Client getClient() { return client; }[m
[32m+[m[32m    public UndergroundDetector getUndergroundDetector() { return undergroundDetector; }[m
 [m
     @Override[m
     protected void startUp()[m
[36m@@ -81,6 +99,7 @@[m [mpublic class SkyCyclePlugin extends Plugin[m
 [m
         overlayManager.add(overlay);[m
         overlayManager.add(sceneTintOverlay);[m
[32m+[m[32m        keyManager.registerKeyListener(toggleAreaHotkey);[m
 [m
         if (hdBridge.isHdActive())[m
         {[m
[36m@@ -100,6 +119,7 @@[m [mpublic class SkyCyclePlugin extends Plugin[m
         log.info("Divine: Skies shutting down");[m
         overlayManager.remove(overlay);[m
         overlayManager.remove(sceneTintOverlay);[m
[32m+[m[32m        keyManager.unregisterKeyListener(toggleAreaHotkey);[m
 [m
         if (initialized)[m
         {[m
[36m@@ -162,6 +182,35 @@[m [mpublic class SkyCyclePlugin extends Plugin[m
 [m
         currentlyUnderground = underground;[m
         currentlyInPoh = inPoh && config.pohEnabled();[m
[32m+[m
[32m+[m[32m        applyShadowState(underground, inPoh);[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * Force the configured shadow mode during the NIGHT phase on the surface,[m
[32m+[m[32m     * and restore the user's 117 HD baseline everywhere else (day, transitions,[m
[32m+[m[32m     * underground, POH). HdBridge skips redundant writes so this is tick-safe.[m
[32m+[m[32m     */[m
[32m+[m[32m    private void applyShadowState(boolean underground, boolean inPoh)[m
[32m+[m[32m    {[m
[32m+[m[32m        if (!initialized)[m
[32m+[m[32m        {[m
[32m+[m[32m            return;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        NightShadowMode mode = config.nightShadowMode();[m
[32m+[m[32m        boolean nightOnSurface = !underground && !inPoh[m
[32m+[m[32m            && cycleEngine != null[m
[32m+[m[32m            && cycleEngine.getCurrentPhase() == CyclePhase.NIGHT;[m
[32m+[m
[32m+[m[32m        if (nightOnSurface && mode != NightShadowMode.UNCHANGED)[m
[32m+[m[32m        {[m
[32m+[m[32m            hdBridge.applyShadowMode(mode.getHdValue());[m
[32m+[m[32m        }[m
[32m+[m[32m        else[m
[32m+[m[32m        {[m
[32m+[m[32m            hdBridge.applyShadowMode(null); // restore user's baseline[m
[32m+[m[32m        }[m
     }[m
 [m
     @Subscribe[m
[36m@@ -171,6 +220,16 @@[m [mpublic class SkyCyclePlugin extends Plugin[m
 [m
         String key = event.getKey();[m
 [m
[32m+[m[32m        // "Clear All Overrides" acts as a button: when switched on, do the work and reset it.[m
[32m+[m[32m        if ("clearAllOverrides".equals(key) && config.clearAllOverrides())[m
[32m+[m[32m        {[m
[32m+[m[32m            int cleared = undergroundDetector.clearAllOverrides();[m
[32m+[m[32m            configManager.setConfiguration(SkyCycleConfig.CONFIG_GROUP, "clearAllOverrides", false);[m
[32m+[m[32m            sendChat("Cleared " + cleared + " region override" + (cleared == 1 ? "" : "s") + ".");[m
[32m+[m[32m            forceVisualRefresh();[m
[32m+[m[32m            return;[m
[32m+[m[32m        }[m
[32m+[m
         // Only reconfigure the engine for timing-related changes — not overlay/tint/color toggles[m
         if (isTimingKey(key))[m
         {[m
[36m@@ -186,6 +245,55 @@[m [mpublic class SkyCyclePlugin extends Plugin[m
         tickCounter = UPDATE_INTERVAL_TICKS;[m
     }[m
 [m
[32m+[m[32m    /**[m
[32m+[m[32m     * Cycle the current region's manual override and apply the result immediately.[m
[32m+[m[32m     * Runs on the client thread (invoked from the hotkey listener).[m
[32m+[m[32m     */[m
[32m+[m[32m    private void handleToggleArea()[m
[32m+[m[32m    {[m
[32m+[m[32m        if (client.getGameState() != GameState.LOGGED_IN)[m
[32m+[m[32m        {[m
[32m+[m[32m            return;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        String result = undergroundDetector.cycleOverrideAtCurrentLocation();[m
[32m+[m[32m        if (result == null)[m
[32m+[m[32m        {[m
[32m+[m[32m            // Overrides disabled, or no region resolvable (e.g. mid-load)[m
[32m+[m[32m            if (!config.allowManualOverrides())[m
[32m+[m[32m            {[m
[32m+[m[32m                sendChat("Manual overrides are disabled (enable them in the Divine: Skies settings).");[m
[32m+[m[32m            }[m
[32m+[m[32m            return;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        if (config.overrideChatFeedback())[m
[32m+[m[32m        {[m
[32m+[m[32m            sendChat(result);[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        forceVisualRefresh();[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * Force the next tick to re-evaluate location and re-apply the sky immediately,[m
[32m+[m[32m     * bypassing the throttle interval.[m
[32m+[m[32m     */[m
[32m+[m[32m    private void forceVisualRefresh()[m
[32m+[m[32m    {[m
[32m+[m[32m        lastAppliedColor = null;   // invalidate the "skip redundant write" cache[m
[32m+[m[32m        tickCounter = UPDATE_INTERVAL_TICKS;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    private void sendChat(String message)[m
[32m+[m[32m    {[m
[32m+[m[32m        if (client.getGameState() == GameState.LOGGED_IN)[m
[32m+[m[32m        {[m
[32m+[m[32m            client.addChatMessage(ChatMessageType.GAMEMESSAGE, "",[m
[32m+[m[32m                "[Divine: Skies] " + message, null);[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
[32m+[m
     private static boolean isTimingKey(String key)[m
     {[m
         switch (key)[m
[1mdiff --git a/src/main/java/com/skycycle/UndergroundDetector.java b/src/main/java/com/skycycle/UndergroundDetector.java[m
[1mindex 33416d8..49c9899 100644[m
[1m--- a/src/main/java/com/skycycle/UndergroundDetector.java[m
[1m+++ b/src/main/java/com/skycycle/UndergroundDetector.java[m
[36m@@ -14,38 +14,95 @@[m
 [m
 package com.skycycle;[m
 [m
[32m+[m[32mimport com.google.gson.Gson;[m
[32m+[m[32mimport com.google.gson.reflect.TypeToken;[m
 import net.runelite.api.Client;[m
[32m+[m[32mimport net.runelite.api.Player;[m
[32m+[m[32mimport net.runelite.api.coords.LocalPoint;[m
 import net.runelite.api.coords.WorldPoint;[m
[32m+[m[32mimport net.runelite.client.config.ConfigManager;[m
 import org.slf4j.Logger;[m
 import org.slf4j.LoggerFactory;[m
 [m
 import javax.inject.Inject;[m
 import javax.inject.Singleton;[m
[32m+[m[32mimport java.lang.reflect.Type;[m
 import java.util.Arrays;[m
[32m+[m[32mimport java.util.Collections;[m
[32m+[m[32mimport java.util.HashMap;[m
 import java.util.HashSet;[m
[32m+[m[32mimport java.util.Map;[m
 import java.util.Set;[m
 [m
 /**[m
[31m- * Detects whether the player is underground, in Death's Office, or in a Player-Owned House.[m
[32m+[m[32m * Classifies the player's current location as SURFACE, UNDERGROUND or POH.[m
  *[m
[31m- * Detection uses three layers for underground:[m
[31m- * 1. FORCE_SURFACE_REGIONS - always treated as surface, even if Y > 8000.[m
[31m- * 2. FORCE_UNDERGROUND_REGIONS - always underground (no surface overlap).[m
[31m- * 3. INSTANCE_UNDERGROUND_REGIONS - underground ONLY when instanced or non-zero plane.[m
[31m- * 4. Y coordinate fallback - Y > 8000 = underground.[m
[32m+[m[32m * <p>Detection runs through a prioritised chain. Each step short-circuits the rest:</p>[m
  *[m
[31m- * POH detection uses chunk coordinate range matching with an exclusion set for[m
[31m- * non-POH instanced regions that fall within the same coordinate space.[m
[32m+[m[32m * <ol>[m
[32m+[m[32m *   <li><b>Varbit gates</b> — server-authoritative state (CoX / ToB / ToA / POH build[m
[32m+[m[32m *       mode). These cannot be wrong, so they sit above the player override layer.</li>[m
[32m+[m[32m *   <li><b>Player overrides</b> — manual per-region classifications set by the player[m
[32m+[m[32m *       with the toggle hotkey. Persisted in our own config namespace.</li>[m
[32m+[m[32m *   <li><b>POH template regions</b> — the eight verified POH instance template region[m
[32m+[m[32m *       IDs. Covers all 9 portal cities and all house sizes (1x1 through 8x8).</li>[m
[32m+[m[32m *   <li><b>FORCE_SURFACE_REGIONS</b> — regions always treated as surface.</li>[m
[32m+[m[32m *   <li><b>FORCE_UNDERGROUND_REGIONS</b> — regions always underground (no surface[m
[32m+[m[32m *       overlap, so the region ID alone is sufficient).</li>[m
[32m+[m[32m *   <li><b>INSTANCE_UNDERGROUND_REGIONS</b> — regions that are underground only when[m
[32m+[m[32m *       the player is in an instance (they share chunk coordinates with surface areas).</li>[m
[32m+[m[32m *   <li><b>Y coordinate fallback</b> — Y &gt;= 6400 = underground. This is the real[m
[32m+[m[32m *       OSRS "underground band" boundary (the previous 8000 threshold missed a large[m
[32m+[m[32m *       slice of underground content between 6400 and 8000).</li>[m
[32m+[m[32m * </ol>[m
[32m+[m[32m *[m
[32m+[m[32m * <p>All coordinate and region lookups are <b>instance-aware</b>: in instanced areas[m
[32m+[m[32m * (raids, POH, Gauntlet, etc.) the raw world location is a meaningless pseudo-coordinate,[m
[32m+[m[32m * so we resolve the canonical template coordinate with[m
[32m+[m[32m * {@link WorldPoint#fromLocalInstance(Client, LocalPoint)} first. This is the same[m
[32m+[m[32m * approach RuneLite's own GroundMarker and Discord plugins use.</p>[m
[32m+[m[32m *[m
[32m+[m[32m * <p>To find new region IDs: enable RuneLite's "Developer Tools" plugin, or turn on[m
[32m+[m[32m * Divine: Skies' own Developer Mode in the overlay. Then either add the ID to one of[m
[32m+[m[32m * the sets below, or simply use the in-game toggle hotkey to set a persistent override.</p>[m
  */[m
 @Singleton[m
 public class UndergroundDetector[m
 {[m
     private static final Logger log = LoggerFactory.getLogger(UndergroundDetector.class);[m
[31m-    private static final int UNDERGROUND_Y_THRESHOLD = 8000;[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * The OSRS underground "band" begins at Y = 6400. Almost all dungeon/cave content[m
[32m+[m[32m     * lives at or above this line. (The previous threshold of 8000 left a gap.)[m
[32m+[m[32m     *[m
[32m+[m[32m     * NOTE: a handful of areas above this line are visually outdoors — Zanaris (~4400),[m
[32m+[m[32m     * Puro-Puro (~4300) are actually below it, but POH templates (~5200-5700) and some[m
[32m+[m[32m     * instance pseudo-coords can land above it. Those are caught by earlier layers[m
[32m+[m[32m     * (POH region match, FORCE_SURFACE_REGIONS) before the fallback ever runs.[m
[32m+[m[32m     */[m
[32m+[m[32m    private static final int UNDERGROUND_Y_THRESHOLD = 6400;[m
[32m+[m
[32m+[m[32m    // --- Varbit IDs (server-authoritative; integer IDs are stable across RL versions) ---[m
[32m+[m[32m    private static final int VARBIT_IN_RAID = 5432;        // Chambers of Xeric: 1 while in a raid[m
[32m+[m[32m    private static final int VARBIT_THEATRE_OF_BLOOD = 6440; // ToB: >=2 inside/spectating[m
[32m+[m[32m    private static final int VARBIT_TOA_RAID_LEVEL = 14380;  // ToA: >0 invocation level when inside[m
[32m+[m[32m    private static final int VARBIT_POH_BUILD_MODE = 2176;   // 1 while in POH build mode[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * The eight POH instance template region IDs. Once the player is INSIDE a POH[m
[32m+[m[32m     * instance, the portal city is irrelevant — the instance always uses these[m
[32m+[m[32m     * template regions regardless of which of the 9 portals was used or the house size.[m
[32m+[m[32m     *[m
[32m+[m[32m     * These are the same IDs RuneLite hardcodes internally as REGION_POH.[m
[32m+[m[32m     */[m
[32m+[m[32m    private static final Set<Integer> POH_TEMPLATE_REGIONS = Collections.unmodifiableSet([m
[32m+[m[32m        new HashSet<>(Arrays.asList([m
[32m+[m[32m            7513, 7514, 7769, 7770, 8025, 8026, 8281, 8282[m
[32m+[m[32m        )));[m
 [m
     /**[m
      * Regions that are ALWAYS underground. These have chunk coordinates that don't[m
[31m-     * overlap with any surface-world area, so region ID alone is sufficient.[m
[32m+[m[32m     * overlap with any surface-world area, so the region ID alone is sufficient.[m
      */[m
     private static final Set<Integer> FORCE_UNDERGROUND_REGIONS = new HashSet<>(Arrays.asList([m
         6222,   // Death's Office / Death's Domain[m
[36m@@ -60,159 +117,358 @@[m [mpublic class UndergroundDetector[m
         11836,  // Blast Furnace[m
         11844,  // Corporeal Beast Cave[m
         12126,  // Lithkren Vault[m
[31m-        12127,  // Ancient Guthixian Temple[m
[32m+[m[32m        12127,  // Ancient Guthixian Temple / Gauntlet lobby[m
         12192,  // Camdozaal / Rogues' Den[m
         12933,  // Zalcano[m
         13654   // Leviathan Path[m
     ));[m
 [m
     /**[m
[31m-     * Regions that are underground ONLY when the player is in an instance or on a[m
[31m-     * non-ground plane. These share chunk coordinates with surface-world areas.[m
[32m+[m[32m     * Regions that are underground ONLY when the player is in an instance. These share[m
[32m+[m[32m     * chunk coordinates with surface-world areas, so the region ID alone would cause[m
[32m+[m[32m     * false positives.[m
[32m+[m[32m     *[m
[32m+[m[32m     * NB: several entries from earlier builds were flagged during research as[m
[32m+[m[32m     * questionable region IDs. They are kept here (so behaviour doesn't silently change[m
[32m+[m[32m     * for existing users) but marked. Verify against the live cache / Developer Mode[m
[32m+[m[32m     * before trusting them, or let player overrides correct them in the field.[m
      */[m
     private static final Set<Integer> INSTANCE_UNDERGROUND_REGIONS = new HashSet<>(Arrays.asList([m
         9781,   // Tree Gnome Village Maze Cave[m
         11050,  // Ape Atoll Cooking Cave[m
[31m-        11058,  // Lassar Undercity[m
[31m-        12132,  // Duke Sucellus[m
[31m-        12598,  // Burthorpe Games Room[m
[32m+[m[32m        11058,  // (flagged) reported as Lassar Undercity — verify; may be surface Brimhaven[m
[32m+[m[32m        12132,  // Duke Sucellus / Lassar Undercity area[m
[32m+[m[32m        12598,  // (flagged) reported as Burthorpe Games Room — verify region ID[m
         13107,  // Abandoned Mine 2nd floor[m
[31m-        14385   // Phantom Muspah Arena[m
[32m+[m[32m        14385   // (flagged) reported as Phantom Muspah — RL uses 11330 for Muspah; verify[m
     ));[m
 [m
     /**[m
[31m-     * Regions that should ALWAYS be treated as surface/overworld, even if the[m
[31m-     * Y coordinate would otherwise flag them as underground.[m
[32m+[m[32m     * Regions that should ALWAYS be treated as surface/overworld, even if a later layer[m
[32m+[m[32m     * (e.g. the Y fallback) would otherwise flag them as underground.[m
      */[m
     private static final Set<Integer> FORCE_SURFACE_REGIONS = new HashSet<>(Arrays.asList([m
[31m-        10041   // Reported: needs overworld sky[m
[32m+[m[32m        10041   // Reported: needs overworld sky (north Fremennik coastline area)[m
     ));[m
 [m
[31m-    /**[m
[31m-     * Instanced regions that fall within the POH chunk coordinate space[m
[31m-     * but are NOT player houses. These must be explicitly excluded from[m
[31m-     * POH detection to prevent false positives.[m
[31m-     */[m
[31m-    private static final Set<Integer> POH_EXCLUSION_REGIONS = new HashSet<>(Arrays.asList([m
[31m-        8023    // Demonic Gorilla Cave (chunkX=31, chunkY=87, instanced)[m
[31m-    ));[m
[32m+[m[32m    /** Config key under which the serialised override map is persisted. */[m
[32m+[m[32m    private static final String OVERRIDE_KEY = "regionOverrides";[m
 [m
     private final Client client;[m
[32m+[m[32m    private final ConfigManager configManager;[m
[32m+[m[32m    private final SkyCycleConfig config;[m
[32m+[m[32m    private final Gson gson;[m
[32m+[m
[32m+[m[32m    /** region ID -> forced AreaType. Loaded from config on startup. */[m
[32m+[m[32m    private final Map<Integer, AreaType> overrides = new HashMap<>();[m
 [m
     @Inject[m
[31m-    public UndergroundDetector(Client client)[m
[32m+[m[32m    public UndergroundDetector(Client client, ConfigManager configManager, SkyCycleConfig config, Gson gson)[m
     {[m
         this.client = client;[m
[32m+[m[32m        this.configManager = configManager;[m
[32m+[m[32m        this.config = config;[m
[32m+[m[32m        this.gson = gson;[m
[32m+[m[32m        loadOverrides();[m
     }[m
 [m
[32m+[m[32m    // =====================================================================[m
[32m+[m[32m    // Public API (unchanged signatures so the plugin needs minimal edits)[m
[32m+[m[32m    // =====================================================================[m
[32m+[m
     /**[m
[31m-     * Check if the player is in a Player-Owned House instance.[m
[31m-     * POH instances occupy chunk coordinates X=24-33, Y=75-120. This covers[m
[31m-     * all house sizes (1x1 through 8x8), all floors (ground/upper/dungeon),[m
[31m-     * and all 9 portal locations (they all use the same instance space).[m
[31m-     *[m
[31m-     * The exclusion set prevents false positives from non-POH instanced regions[m
[31m-     * that happen to fall within the same coordinate range (e.g. Demonic Gorillas).[m
[32m+[m[32m     * @return true if the player should currently see an underground (dark) sky.[m
[32m+[m[32m     */[m
[32m+[m[32m    public boolean isUnderground()[m
[32m+[m[32m    {[m
[32m+[m[32m        return classify() == AreaType.UNDERGROUND;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * @return true if the player is in a Player-Owned House.[m
      */[m
     public boolean isInPlayerHouse()[m
     {[m
[31m-        if (!client.isInInstancedRegion()) return false;[m
[32m+[m[32m        return classify() == AreaType.POH;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * Full classification of the current location.[m
[32m+[m[32m     *[m
[32m+[m[32m     * @return SURFACE, UNDERGROUND or POH; defaults to SURFACE if state is unknown.[m
[32m+[m[32m     */[m
[32m+[m[32m    public AreaType classify()[m
[32m+[m[32m    {[m
[32m+[m[32m        Player local = client.getLocalPlayer();[m
[32m+[m[32m        if (local == null)[m
[32m+[m[32m        {[m
[32m+[m[32m            return AreaType.SURFACE;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        // 1. Server-authoritative varbit gates. A confirmed raid / build session[m
[32m+[m[32m        //    cannot be overridden by region heuristics OR by player overrides.[m
[32m+[m[32m        if (safeVarbit(VARBIT_IN_RAID) == 1) return AreaType.UNDERGROUND;          // CoX[m
[32m+[m[32m        if (safeVarbit(VARBIT_THEATRE_OF_BLOOD) >= 2) return AreaType.UNDERGROUND; // ToB[m
[32m+[m[32m        if (safeVarbit(VARBIT_TOA_RAID_LEVEL) > 0) return AreaType.UNDERGROUND;    // ToA[m
[32m+[m[32m        if (safeVarbit(VARBIT_POH_BUILD_MODE) == 1) return AreaType.POH;           // POH build[m
 [m
[31m-        int[] regions = client.getMapRegions();[m
[31m-        if (regions == null) return false;[m
[32m+[m[32m        int region = resolveRegionId();[m
 [m
[31m-        // Exclusion check first — bail if any loaded region is a known non-POH instance[m
[31m-        for (int region : regions)[m
[32m+[m[32m        // 2. Player override layer — wins over all region-based heuristics below.[m
[32m+[m[32m        if (region >= 0)[m
         {[m
[31m-            if (POH_EXCLUSION_REGIONS.contains(region))[m
[32m+[m[32m            AreaType override = overrides.get(region);[m
[32m+[m[32m            if (override != null)[m
             {[m
[31m-                return false;[m
[32m+[m[32m                return override;[m
             }[m
         }[m
 [m
[31m-        // Range check — POH chunks fall in X=24-33, Y=75-120[m
[31m-        for (int region : regions)[m
[32m+[m[32m        // 3. POH template regions.[m
[32m+[m[32m        if (region >= 0 && POH_TEMPLATE_REGIONS.contains(region))[m
         {[m
[31m-            int chunkX = region >> 8;[m
[31m-            int chunkY = region & 0xFF;[m
[32m+[m[32m            return AreaType.POH;[m
[32m+[m[32m        }[m
 [m
[31m-            if (chunkX >= 24 && chunkX <= 33 && chunkY >= 75 && chunkY <= 120)[m
[31m-            {[m
[31m-                return true;[m
[31m-            }[m
[32m+[m[32m        // 4. Force-surface.[m
[32m+[m[32m        if (region >= 0 && FORCE_SURFACE_REGIONS.contains(region))[m
[32m+[m[32m        {[m
[32m+[m[32m            return AreaType.SURFACE;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        // 5. Force-underground (no surface overlap).[m
[32m+[m[32m        if (region >= 0 && FORCE_UNDERGROUND_REGIONS.contains(region))[m
[32m+[m[32m        {[m
[32m+[m[32m            return AreaType.UNDERGROUND;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        // 6. Instance-only underground regions.[m
[32m+[m[32m        if (region >= 0 && client.isInInstancedRegion()[m
[32m+[m[32m            && INSTANCE_UNDERGROUND_REGIONS.contains(region))[m
[32m+[m[32m        {[m
[32m+[m[32m            return AreaType.UNDERGROUND;[m
         }[m
[31m-        return false;[m
[32m+[m
[32m+[m[32m        // 7. Y coordinate fallback.[m
[32m+[m[32m        WorldPoint wp = resolveWorldPoint();[m
[32m+[m[32m        if (wp != null && wp.getY() >= UNDERGROUND_Y_THRESHOLD)[m
[32m+[m[32m        {[m
[32m+[m[32m            return AreaType.UNDERGROUND;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        return AreaType.SURFACE;[m
     }[m
 [m
[32m+[m[32m    // =====================================================================[m
[32m+[m[32m    // Player override system[m
[32m+[m[32m    // =====================================================================[m
[32m+[m
     /**[m
[31m-     * Determine if the player is currently in an underground/dungeon area.[m
[32m+[m[32m     * Cycle the override for the player's CURRENT region through:[m
[32m+[m[32m     * (none) -> UNDERGROUND -> SURFACE -> POH -> (none).[m
[32m+[m[32m     *[m
[32m+[m[32m     * <p>Must be called on the client thread (region/coordinate reads are not[m
[32m+[m[32m     * thread-safe).</p>[m
      *[m
[31m-     * Priority:[m
[31m-     * 1. POH check is handled separately by the caller[m
[31m-     * 2. Force-surface override - NOT underground[m
[31m-     * 3. Force-underground (safe regions) - underground[m
[31m-     * 4. Instance-underground (ambiguous regions + instance check) - underground[m
[31m-     * 5. Y coordinate fallback - Y > 8000 = underground[m
[32m+[m[32m     * @return a human-readable description of the new state, or null if no region[m
[32m+[m[32m     *         could be resolved (e.g. not logged in).[m
      */[m
[31m-    public boolean isUnderground()[m
[32m+[m[32m    public String cycleOverrideAtCurrentLocation()[m
     {[m
[31m-        if (client.getLocalPlayer() == null) return false;[m
[32m+[m[32m        if (!config.allowManualOverrides())[m
[32m+[m[32m        {[m
[32m+[m[32m            return null;[m
[32m+[m[32m        }[m
 [m
[31m-        int[] regions = client.getMapRegions();[m
[32m+[m[32m        int region = resolveRegionId();[m
[32m+[m[32m        if (region < 0)[m
[32m+[m[32m        {[m
[32m+[m[32m            return null;[m
[32m+[m[32m        }[m
 [m
[31m-        if (regions != null)[m
[32m+[m[32m        AreaType current = overrides.get(region);[m
[32m+[m[32m        AreaType next;[m
[32m+[m[32m        if (current == null)[m
         {[m
[31m-            // 1. Force-surface takes highest priority[m
[31m-            for (int region : regions)[m
[32m+[m[32m            next = AreaType.UNDERGROUND;[m
[32m+[m[32m        }[m
[32m+[m[32m        else[m
[32m+[m[32m        {[m
[32m+[m[32m            switch (current)[m
             {[m
[31m-                if (FORCE_SURFACE_REGIONS.contains(region))[m
[31m-                {[m
[31m-                    return false;[m
[31m-                }[m
[32m+[m[32m                case UNDERGROUND:[m
[32m+[m[32m                    next = AreaType.SURFACE;[m
[32m+[m[32m                    break;[m
[32m+[m[32m                case SURFACE:[m
[32m+[m[32m                    next = AreaType.POH;[m
[32m+[m[32m                    break;[m
[32m+[m[32m                case POH:[m
[32m+[m[32m                default:[m
[32m+[m[32m                    next = null; // clear -> back to automatic detection[m
[32m+[m[32m                    break;[m
             }[m
[32m+[m[32m        }[m
 [m
[31m-            // 2. Safe force-underground - no surface overlap[m
[31m-            for (int region : regions)[m
[31m-            {[m
[31m-                if (FORCE_UNDERGROUND_REGIONS.contains(region))[m
[31m-                {[m
[31m-                    return true;[m
[31m-                }[m
[31m-            }[m
[32m+[m[32m        if (next == null)[m
[32m+[m[32m        {[m
[32m+[m[32m            overrides.remove(region);[m
[32m+[m[32m        }[m
[32m+[m[32m        else[m
[32m+[m[32m        {[m
[32m+[m[32m            overrides.put(region, next);[m
[32m+[m[32m        }[m
[32m+[m[32m        saveOverrides();[m
[32m+[m
[32m+[m[32m        return next == null[m
[32m+[m[32m            ? "Region " + region + " override cleared (auto-detect)"[m
[32m+[m[32m            : "Region " + region + " set to " + next.getDisplayName();[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * Remove every saved override. Returns the number cleared.[m
[32m+[m[32m     */[m
[32m+[m[32m    public int clearAllOverrides()[m
[32m+[m[32m    {[m
[32m+[m[32m        int n = overrides.size();[m
[32m+[m[32m        overrides.clear();[m
[32m+[m[32m        saveOverrides();[m
[32m+[m[32m        return n;[m
[32m+[m[32m    }[m
 [m
[31m-            // 3. Ambiguous regions - only count as underground if instanced[m
[31m-            boolean isInstance = client.isInInstancedRegion();[m
[32m+[m[32m    /**[m
[32m+[m[32m     * @return the override for the current region, or null if none / not resolvable.[m
[32m+[m[32m     */[m
[32m+[m[32m    public AreaType getCurrentOverride()[m
[32m+[m[32m    {[m
[32m+[m[32m        int region = resolveRegionId();[m
[32m+[m[32m        return region < 0 ? null : overrides.get(region);[m
[32m+[m[32m    }[m
 [m
[31m-            if (isInstance)[m
[32m+[m[32m    public int getOverrideCount()[m
[32m+[m[32m    {[m
[32m+[m[32m        return overrides.size();[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    private void loadOverrides()[m
[32m+[m[32m    {[m
[32m+[m[32m        overrides.clear();[m
[32m+[m[32m        String json = configManager.getConfiguration(SkyCycleConfig.CONFIG_GROUP, OVERRIDE_KEY);[m
[32m+[m[32m        if (json == null || json.isEmpty())[m
[32m+[m[32m        {[m
[32m+[m[32m            return;[m
[32m+[m[32m        }[m
[32m+[m[32m        try[m
[32m+[m[32m        {[m
[32m+[m[32m            Type type = new TypeToken<Map<Integer, AreaType>>() {}.getType();[m
[32m+[m[32m            Map<Integer, AreaType> saved = gson.fromJson(json, type);[m
[32m+[m[32m            if (saved != null)[m
             {[m
[31m-                for (int region : regions)[m
[31m-                {[m
[31m-                    if (INSTANCE_UNDERGROUND_REGIONS.contains(region))[m
[31m-                    {[m
[31m-                        return true;[m
[31m-                    }[m
[31m-                }[m
[32m+[m[32m                overrides.putAll(saved);[m
[32m+[m[32m                log.debug("Loaded {} region override(s)", overrides.size());[m
             }[m
         }[m
[32m+[m[32m        catch (Exception e)[m
[32m+[m[32m        {[m
[32m+[m[32m            log.warn("Failed to parse saved region overrides; starting fresh", e);[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
 [m
[31m-        // 4. Fallback: standard Y coordinate check[m
[31m-        WorldPoint wp = client.getLocalPlayer().getWorldLocation();[m
[31m-        if (wp == null) return false;[m
[32m+[m[32m    private void saveOverrides()[m
[32m+[m[32m    {[m
[32m+[m[32m        try[m
[32m+[m[32m        {[m
[32m+[m[32m            String json = gson.toJson(overrides);[m
[32m+[m[32m            configManager.setConfiguration(SkyCycleConfig.CONFIG_GROUP, OVERRIDE_KEY, json);[m
[32m+[m[32m        }[m
[32m+[m[32m        catch (Exception e)[m
[32m+[m[32m        {[m
[32m+[m[32m            log.warn("Failed to persist region overrides", e);[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
 [m
[31m-        return wp.getY() > UNDERGROUND_Y_THRESHOLD;[m
[32m+[m[32m    // =====================================================================[m
[32m+[m[32m    // Instance-aware coordinate / region resolution[m
[32m+[m[32m    // =====================================================================[m
[32m+[m
[32m+[m[32m    /**[m
[32m+[m[32m     * Resolve the player's true region ID, accounting for instances.[m
[32m+[m[32m     *[m
[32m+[m[32m     * <p>In an instanced area the raw world location is a pseudo-coordinate that does[m
[32m+[m[32m     * not correspond to the real template region, so we decode the template chunk via[m
[32m+[m[32m     * {@link WorldPoint#fromLocalInstance}. This call can throw[m
[32m+[m[32m     * ArrayIndexOutOfBoundsException if invoked mid-load (see RL issues #18681/#1667),[m
[32m+[m[32m     * so it is guarded.</p>[m
[32m+[m[32m     *[m
[32m+[m[32m     * @return region ID, or -1 if it could not be resolved.[m
[32m+[m[32m     */[m
[32m+[m[32m    public int resolveRegionId()[m
[32m+[m[32m    {[m
[32m+[m[32m        WorldPoint wp = resolveWorldPoint();[m
[32m+[m[32m        return wp == null ? -1 : wp.getRegionID();[m
     }[m
 [m
     /**[m
[31m-     * Get the current region IDs for debug/logging purposes.[m
[32m+[m[32m     * Resolve the player's canonical WorldPoint, instance-aware and crash-guarded.[m
      */[m
[32m+[m[32m    private WorldPoint resolveWorldPoint()[m
[32m+[m[32m    {[m
[32m+[m[32m        Player local = client.getLocalPlayer();[m
[32m+[m[32m        if (local == null)[m
[32m+[m[32m        {[m
[32m+[m[32m            return null;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        if (!client.isInInstancedRegion())[m
[32m+[m[32m        {[m
[32m+[m[32m            return local.getWorldLocation();[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        // Instanced: decode the template coordinate. Guard against transient[m
[32m+[m[32m        // mid-load states that can index out of bounds.[m
[32m+[m[32m        try[m
[32m+[m[32m        {[m
[32m+[m[32m            LocalPoint lp = local.getLocalLocation();[m
[32m+[m[32m            if (lp == null)[m
[32m+[m[32m            {[m
[32m+[m[32m                return local.getWorldLocation();[m
[32m+[m[32m            }[m
[32m+[m[32m            return WorldPoint.fromLocalInstance(client, lp);[m
[32m+[m[32m        }[m
[32m+[m[32m        catch (Exception e)[m
[32m+[m[32m        {[m
[32m+[m[32m            log.debug("fromLocalInstance failed (transient load state?); falling back", e);[m
[32m+[m[32m            return local.getWorldLocation();[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    private int safeVarbit(int id)[m
[32m+[m[32m    {[m
[32m+[m[32m        try[m
[32m+[m[32m        {[m
[32m+[m[32m            return client.getVarbitValue(id);[m
[32m+[m[32m        }[m
[32m+[m[32m        catch (Exception e)[m
[32m+[m[32m        {[m
[32m+[m[32m            return 0;[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    // =====================================================================[m
[32m+[m[32m    // Debug / overlay helpers[m
[32m+[m[32m    // =====================================================================[m
[32m+[m
[32m+[m[32m    /** Raw loaded regions (for the dev overlay). */[m
     public int[] getCurrentRegions()[m
     {[m
         return client.getMapRegions();[m
     }[m
 [m
[31m-    /**[m
[31m-     * Check if the current area is an instance.[m
[31m-     */[m
[32m+[m[32m    /** Instance-aware resolved region (for the dev overlay). */[m
[32m+[m[32m    public int getResolvedRegion()[m
[32m+[m[32m    {[m
[32m+[m[32m        return resolveRegionId();[m
[32m+[m[32m    }[m
[32m+[m
     public boolean isInInstance()[m
     {[m
         return client.isInInstancedRegion();[m
